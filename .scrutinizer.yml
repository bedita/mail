checks:
  php: true
filter:
  paths:
    - 'src/*'
  dependency_paths:
    - 'vendor/*'

build:
  image: default-jammy
  environment:
    node: v24
  nodes:
    analysis:
      environment:
        php:
          version: 8.3.27
      tests:
        override:
          - php-scrutinizer-run
